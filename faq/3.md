# Úkol 3

## Jaký typ SQL dotazu se použije ve vaší aplikaci pro založení nového záznamu do datasetu?
Pro založení nového záznamu se používá `INSERT` dotaz, konkrétně `INSERT INTO ptaci ... VALUES (...)`.

## Napište dekorátor, který používá funkce implementující obsluhu mazání záznamů z databáze.
Pro mazání záznamů se používá dekorátor `@login_required`.

```python
@app.route("/birds/<int:bird_id>/delete", methods=["POST"])
@login_required
def delete_bird(bird_id):
    ...
```

## Popište, jakým způsobem se nahrají do databáze počáteční data. Neodpovídejte však triviálním tvrzením, např. spustí se tento skript. Odpovězte, kde nebo jak jsou zdrojová data uložena, a co dělá skript, který je importuje do databáze.
Zdrojová data jsou uložena v CSV souboru [dataset_ptaci_final.csv](dataset_ptaci_final.csv). Import zajišťuje [import_csv.py](import_csv.py): připojí se k databázi `ptaci.db`, smaže a znovu vytvoří tabulku `ptaci`, načte CSV přes `csv.DictReader`, převádí textové hodnoty na správné typy a postupně je vkládá pomocí parametrizovaného `INSERT` dotazu.

```python
with open("dataset_ptaci_final.csv", mode="r", encoding="utf-8-sig") as f:
    reader = csv.DictReader(f)
    for row in reader:
        cursor.execute("""
            INSERT INTO ptaci (
                nazev, vedecky_nazev, rad, celed, delka_cm,
                rozpeti_cm, hmotnost_g, status_ohrozeni,
                typ_potravy, migrace, vyskyt_kontinent, snuska_ks
            ) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
        """, (...))
```

## Jak jste vyřešili přístup uživatelů do aplikace?
Aplikace má registraci, přihlášení a odhlášení. Po úspěšném přihlášení se do session uloží `user_id` a dál se podle něj ověřuje přístup k chráněným stránkám.

## Jakým způsobem ukládáte hesla?
Hesla neukládáme v plain textu, ale hashujeme je pomocí `werkzeug.security.generate_password_hash()` a při přihlašování ověřujeme přes `check_password_hash()`.

## Jak je řešeno, že danou operaci, např. úprava záznamu, může provést pouze přihlášený uživatel? Vložte ukázku kódu, který to řeší.
Přístup je řešen kombinací dekorátoru `@login_required` a kontroly vlastnictví záznamu přes `user_id` v databázi.

```python
@app.route("/birds/<int:bird_id>/edit", methods=["GET", "POST"])
@login_required
def edit_bird(bird_id):
    current_user = get_current_user()
    conn = get_db()
    cursor = conn.cursor()

    cursor.execute("SELECT * FROM ptaci WHERE id = ? AND user_id = ?", (bird_id, current_user['id']))
    bird = cursor.fetchone()

    if not bird:
        flash("Pták nebyl nalezen nebo nemáte oprávnění ho upravovat.", "danger")
        conn.close()
        return redirect(url_for("dashboard"))
```